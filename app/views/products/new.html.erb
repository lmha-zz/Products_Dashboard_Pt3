<% content_for :title do %>
  Add a New Product
<% end %>

<h1>New product</h1>

<% if !flash[:errors].nil? %>
  <% flash[:errors].each do |error| %>
  <p class="errors"><%= error %></p>
  <% end %>
<% end %>

<%= render partial: 'form' %>

<%= link_to 'Back', products_path %>